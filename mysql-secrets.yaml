apiVersion : v1
kind : Secret
metadata:
  name : mysql-secrets
data:
  username : root (use command : echo -n 'root' | base64) to get the encrypted data of root
  password : password (use command : echo -n 'password' | base64) to get the encrypted data of password
